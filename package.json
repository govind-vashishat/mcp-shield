{
  "name": "mcp-shield-monorepo",
  "private": true,
  "version": "0.0.0",
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "bun run --filter '*' build",
    "typecheck": "bun run --filter '*' typecheck",
    "shield": "bun run packages/cli/src/index.ts"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "@types/bun": "latest"
  }
}
